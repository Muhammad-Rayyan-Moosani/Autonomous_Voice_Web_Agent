// content.js
// Runs inside every webpage (the "hands" of the agent)
// Can read/modify the DOM, click buttons, scroll, type text
// Sends page context (text, title, URL) to background or popup when requested